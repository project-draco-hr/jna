{
  if (args.length < 2)   this.showCmdHelp();
  for (int i=0; i < args.length; i++) {
    String cmd=args[i];
    if (cmd.startsWith("-")) {
      this.put(args[i].substring(1),args[i + 1]);
    }
  }
}
