{
  Pointer vptr=this.pvInstance.getPointer(0);
  Function func=Function.getFunction(vptr.getPointer(16));
  int hr=func.invokeInt(new Object[]{this.pvInstance,iTInfo,lcid,ppTInfo});
  return new HRESULT(hr);
}
