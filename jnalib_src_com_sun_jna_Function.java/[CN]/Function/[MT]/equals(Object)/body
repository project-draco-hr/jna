{
  if (o instanceof Function) {
    Function other=(Function)o;
    return other.callingConvention == this.callingConvention && other.options.equals(this.options) && other.peer == this.peer;
  }
  return false;
}
