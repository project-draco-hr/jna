{
  TestLibrary.CheckFieldAlignment struct=new TestLibrary.CheckFieldAlignment();
  assertEquals("Native size does not match calculated size",struct.getPointer(),lib.testSimpleStructurePointerArgument(struct));
}
