{
  long[] idx=new long[1];
  idx[0]=index;
  VARIANT result=new VARIANT();
  HRESULT hr=OleAut32.INSTANCE.SafeArrayGetElement(array,idx,result.getPointer());
  COMUtils.SUCCEEDED(hr);
  return result;
}
