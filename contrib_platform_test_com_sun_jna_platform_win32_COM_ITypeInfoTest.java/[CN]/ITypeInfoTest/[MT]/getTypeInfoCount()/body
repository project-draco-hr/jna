{
  IntByReference pctinfo=new IntByReference();
  HRESULT hr=comObj.getIDispatch().GetTypeInfoCount(pctinfo);
  if (pctinfo.getValue() == 0)   throw new RuntimeException("No type information found!");
  return pctinfo.getValue();
}
