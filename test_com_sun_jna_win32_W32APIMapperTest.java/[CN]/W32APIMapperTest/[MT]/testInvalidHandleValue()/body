{
  String expected="opaque@0xffffffff";
  if (Pointer.SIZE == 8) {
    expected+="ffffffff";
  }
  assertEquals("Wrong value",expected,Pointer.createConstant(Pointer.SIZE == 8 ? -1 : 0xFFFFFFFFL).toString());
}
